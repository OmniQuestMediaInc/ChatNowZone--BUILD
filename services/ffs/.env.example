# Room-Heat Engine — Environment Variables

# NATS connection (shared with all services)
NATS_URL=nats://localhost:4222

# Postgres connection (shared Prisma client)
DATABASE_URL=postgresql://cnz_user:changeme@localhost:5432/chatnowzone

# Redis (future — for rolling-window aggregations; not yet used by this service)
REDIS_HOST=localhost
REDIS_PORT=6379
